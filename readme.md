pip install qrcode
pip install pillow (for image processing)

pip install django
django-admin startproject foodsite .
python manage.py startapp
connect the new app to the settings--> installed app of the foodsite.

then can python manage.py runserver

python manage.py makemigrations
python manage.py migrate

We are following class based views here.
